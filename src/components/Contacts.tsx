import React from 'react';
import Contact from './Contact';

const Contacts = () => {
    return (
        <div>
             <Contact name="Rifat" age={20}  />
        </div>
    );
};

export default Contacts;